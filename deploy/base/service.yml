apiVersion: v1
kind: Service
metadata:
  name: cronogramas-mcp
spec:
  selector:
    app: cronogramas-mcp
  ports:
    - port: 3003
      targetPort: 80
      protocol: TCP
      name: http
  type: LoadBalancer